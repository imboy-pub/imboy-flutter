# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/doc/moments_*.md
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.env*
plugin/build
ios/build
macos/build
*.jks
pro_env.dart
main_dev.dart
main_pro.dart
main_local.dart
# 备份产物：按**扩展名**精确匹配，不用 `*bak*` / `*backup*` 宽模式。
#
# 原来是两条宽模式 + 7 行 `!` 白名单打补丁。问题在于白名单要手工维护：
# 每新增一个文件名含 backup 的源文件就得补一行，漏一行该文件就静默地
# 不进版本控制。test/service/e2ee_backup_restore_test.dart 正是漏掉的那个 ——
# 一个真实的测试文件，从未入库，本地跑得好好的，CI 和别人 clone 都没有它。
#
# 实测这两条宽模式只拦到 3 个真实备份产物（assets/example10.db.bak、
# lib/component/locales/locales.g.dart.backup、macos 的 Runner.xcscheme.backup），
# 全部可由扩展名覆盖；其余 9 个匹配项都是 e2ee 备份功能的正常源码。
*.bak
*.backup
lib/config/env_dev.dart
lib/config/env_pro.dart
lib/config/env_local.g.dart
lib/config/env_dev.g.dart
lib/config/env_pro.g.dart
# env_local_home / env_local_office 两个变体此前漏加，导致 envied 生成的
# solidifiedKey（含解混淆用的 XOR 密钥数组，可直接还原明文）随代码入库。
# 约定与上面三条一致：env_*.dart 源文件入库、env_*.g.dart 生成物不入库，
# 各自从本地未入库的 .env.<环境> 用 build_runner 重新生成。
lib/config/env_local_home.g.dart
lib/config/env_local_office.g.dart
IDEWorkspaceChecks.plist
android/build
.trae
.vscode
.codebuddy
.lingma
.kiro
.gradle
.env
.env.dev
.env.pro
.env.local
.env.local_home
.env.local_office
.kilocode
erl_code
# Claude AI - 只忽略个人配置，保留项目共享的 skills
.claude/settings.local.json
.claude/backup/
.claude/debug/
.claude/file-history/
.claude/session-env/
.claude/shell-snapshots/
.claude/todos/
.claude/*.jsonl
.claude/*.json
doc/ai_prompts*

# Automation test reports
test_automation/reports/
android/.kotlin
test_automation/logs
test_automation/.state/
test_automation/.state_yaml/
test_output/
*.skip
plugin/jverify
.claude/
.reports

# Firebase config (contains project secrets)
android/app/google-services.json

# Backend Erlang crash dumps (should not be tracked in Flutter repo)
erl_crash.dump

# Local backups (e.g., pubspec.lock.preloop) — never track
*.preloop

# Slang i18n analyze outputs (regenerated on demand)
assets/i18n/_missing_translations.yaml
assets/i18n/_unused_translations.yaml

# mobile-mcp 真机走查截图（临时产物，勿入库）
.test-screenshots/

# ECC agent 工具产物
.artifacts/
.codex

# Patrol 生成物：patrol_cli 每次运行重新生成的测试入口
patrol_test/
**/.git.disabled/
