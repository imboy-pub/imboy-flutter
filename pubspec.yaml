name: imboy
description: A new Flutter project for IM.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.1.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.5
  # getx 状态管理框架
  get: ^4.6.5
  #演示列表需要用到的库
  dio: ^5.0.1    #网络请求框架
  json_annotation: ^4.8.0 #json序列化和反序列化用的
  permission_handler: ^10.2.0
  camera: ^0.10.3+2
  wechat_camera_picker: ^3.7.0
  #wechat_camera_picker:
  #  git:
  #    url: https://gitee.com/imboy-tripartite-deps/flutter_wechat_camera_picker.git
  #    ref: leeyi
  wechat_assets_picker: ^8.4.0
  # 一组基于 PointyCastle 的高级 API，用于双向加密。 RSA AES
  encrypt: ^5.0.1
  # 在纯 Dart 中实现的一组加密函数。  SHA-* MD5 HMAC
  # crypto: ^3.0.2
  # Flutter 开发者的密码学：加密、数字签名、密钥协议等。
  # AES, Chacha20, ED25519, X25519, and more. Web Crypto support.
  # https://github.com/dint-dev/cryptography
  # cryptography: ^2.0.5
  # cryptography_flutter: ^2.0.2
  # e2ee
  #webcrypto: ^0.5.2
  web_socket_channel: ^2.3.0
  # jwt 插件
  jose: ^0.3.3

  # 缓存
  shared_preferences: ^2.0.18
  # flutter_secure_storage: ^7.0.1
  cached_network_image: ^3.2.3
  flutter_cache_manager: ^3.3.0

  logger: ^1.2.2
  sentry_flutter: ^6.21.0
  photo_view: ^0.14.0
  extended_text: ^10.0.1
  extended_text_field: ^11.0.1
  lpinyin: ^2.0.3
  #azlistview: ^2.0.0
  azlistview:
    git:
      url: https://gitee.com/imboy-tripartite-deps/azlistview.git
      ref: leeyi
  # 视频播放器
  video_player: ^2.5.3
  chewie: ^1.4.0
  webview_flutter: ^4.0.6
  image_picker: ^0.8.6+4
  mime: ^1.0.4
  file_picker: ^5.2.5
  #open_file: ^3.2.1
  open_file:
    git:
      url: https://gitee.com/mirrors_crazecoder/open_file.git
      ref: master
  # 是否联网及网络类型
  connectivity_plus: ^3.0.3
  flutter_screenutil: ^5.6.1
  #pull_to_refresh: ^2.0.0
  pull_to_refresh:
    git:
      url: https://gitee.com/imboy-tripartite-deps/flutter_pulltorefresh.git
      ref: leeyi
  #pull_to_refresh_flutter3: ^2.0.1
  package_info_plus: ^3.0.3
  device_info_plus: ^8.1.0
  event_bus: ^2.0.0
  # https://github.com/letsar/flutter_slidable
  flutter_slidable: ^2.0.0
  # 消息提醒的红点效果
  badges: ^3.0.2
  # 聊天气泡
  bubble: ^1.2.1
  xid: ^1.0.12
  fps_monitor: ^2.0.0
  # @ 小部件
  flutter_mentions: ^2.0.1
  # 时间处理
  jiffy: ^5.0.1
  # 用于校正本地系统与时钟源服务器之前的时间
  ntp: ^2.0.0
  emoji_picker_flutter: ^1.5.2
  carousel_slider: ^4.2.1
  flutter_dotenv: ^5.0.2
  photo_manager: ^2.5.2
  video_compress: ^3.1.2
  path_provider: ^2.0.13
  flutter_login: ^4.1.1
  # 进度条
  percent_indicator: ^4.2.3
  json_serializable: ^6.6.1
  # 一个干净和轻量级的加载/吐司小部件
  flutter_easyloading: ^3.0.5
  # 生成闪屏页
  flutter_native_splash: ^2.2.18
  huawei_account: ^6.1.0+304
  # flutter_chat_ui: ^1.6.6
  flutter_chat_ui:
    path: plugin/flutter_chat_ui
  image_crop: ^0.4.1
  #image_crop:
  #  git:
  #    url: https://gitee.com/imboy-tripartite-deps/image_crop.git
  #    ref: leeyi
  # popup_menu: ^1.0.5
  popup_menu:
    git:
      url: https://gitee.com/imboy-tripartite-deps/popup_menu.git
      ref: master
  niku: ^2.3.3
  # 二维码扫描
  mobile_scanner: ^3.0.0
  # mobile_scanner:
  #  git:
  #    url: https://gitee.com/imboy-tripartite-deps/mobile_scanner.git
  #    ref: master
  # 二维码生成
  qr_flutter: ^4.0.0
  url_launcher: ^6.1.10
  image_gallery_saver: ^1.7.1
  sqflite: ^2.2.5
  flutter_local_notifications: 13.0.0
  flutter_app_badger: ^1.5.0
  # 录音
  flutter_sound: ^9.2.13
  flutter_sound_platform_interface: ^9.2.13
  audio_session: ^0.1.13
  # 语音消息播放器
  # voice_message_package: ^0.0.74
  voice_message_package:
    path: plugin/voice_message_package
  #    git:
  #      url: https://gitee.com/imboy-tripartite-deps/voice_message_player.git
  #      ref: 0.0.74+1
  # 可滚动小部件的固定/可变行高提供了滚动到索引的机制
  scroll_to_index: ^3.0.1
  # 封装了基本的图形样式，矩形、圆角矩形、圆形、椭圆形，以及自定义虚线框
  dotted_border: ^2.0.0+3
  flutter_blurhash: ^0.7.0
  markdown: ^7.0.1
  location: ^5.0.0-dev.8
  #flutter_webrtc: ^0.9.23
  flutter_webrtc:
    git:
      url: https://gitee.com/imboy-tripartite-deps/flutter-webrtc.git
      ref: main
  clock: ^1.1.1
  # sip_ua: ^0.5.1
  # sip_ua:
  #   git:
  #     url: https://gitee.com/imboy-tripartite-deps/dart-sip-ua.git
  #     ref: master
  # flutter_login:
  #  path: plugin/flutter_login

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/.env
    - assets/data/region.json
    - assets/images/def_avatar.png
    - assets/images/splash_bg.png
    - assets/images/logo.png
    - assets/images/ic_index_bar_bubble_gray.png

    - assets/images/chat/voice_volume_1.png
    - assets/images/chat/voice_volume_2.png
    - assets/images/chat/voice_volume_3.png
    - assets/images/chat/voice_volume_4.png
    - assets/images/chat/voice_volume_5.png
    - assets/images/chat/voice_volume_6.png
    - assets/images/chat/voice_volume_7.png
    - assets/images/chat/reply_to.png

    - assets/images/conversation/sending.png
    - assets/images/ic_no_data.webp
    - assets/images/right_more.png
    - assets/images/ic_right_arrow_grey.webp

    - assets/images/mine/ic_social_circle.png
    - assets/images/mine/ic_wallet.webp
    - assets/images/mine/ic_card_package.png
    - assets/images/mine/ic_emoji.png
    - assets/images/mine/ic_pay.png
    - assets/images/mine/ic_setting.png
    - assets/images/mine/ic_collections.png
    - assets/images/mine/ic_album.png
