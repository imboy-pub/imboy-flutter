schema_version: 1
case_id: "e2ee_02_service"
feature_id: "e2ee"
feature_name_cn: "端到端加密"
title: "E2EE 服务层单测"
priority: "P0"
automation_mode: "auto"
status: "mapped"

runner:
  action: "flutter_test"
  test_file: "test/service/e2ee_service_test.dart"
  device: "macos"
  timeout_seconds: 300
  retry: 0
  dart_defines: "APP_ENV=local_home"

precondition: "flutter_test_env"
assertions: "exit_code==0,e2ee_service_contract_passed"
ui_ux_checks: "encrypt_decrypt_behavior_consistent"

steps:
  - "准备测试环境并校验前置条件：flutter_test_env"
  - "执行 Flutter 测试：flutter test test/service/e2ee_service_test.dart"
  - "校验断言与 UI/UX 关键点：exit_code==0,e2ee_service_contract_passed | encrypt_decrypt_behavior_consistent"
