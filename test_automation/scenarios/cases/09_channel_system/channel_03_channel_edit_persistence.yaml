schema_version: 1
case_id: "channel_03_channel_edit_persistence"
feature_id: "channel_system"
feature_name_cn: "频道系统"
title: "频道编辑持久化验证"
priority: "P1"
automation_mode: "auto"
status: "mapped"

runner:
  action: "flutter_test"
  test_file: "integration_test/channel/channel_edit_persistence_test.dart"
  device: "macos"
  timeout_seconds: 900
  retry: 1
  dart_defines: "APP_ENV=local_home"

precondition: "flutter_installed,device=macos,channel_admin_ready"
assertions: "exit_code==0,channel_edit_persistence_passed"
ui_ux_checks: "channel_detail_refresh_consistent"

steps:
  - "准备测试环境并校验前置条件：flutter_installed,device=macos,channel_admin_ready"
  - "执行 Flutter 测试：flutter test integration_test/channel/channel_edit_persistence_test.dart"
  - "校验断言与 UI/UX 关键点：exit_code==0,channel_edit_persistence_passed | channel_detail_refresh_consistent"
