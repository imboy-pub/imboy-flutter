schema_version: 1
case_id: "ws_03_message_ack_flow"
feature_id: "websocket_retry_ack"
feature_name_cn: "WebSocket重试确认"
title: "ACK确认流程集成"
priority: "P0"
automation_mode: "auto"
status: "mapped"

runner:
  action: "flutter_test"
  test_file: "test/integration/message_ack_flow_integration_test.dart"
  device: "macos"
  timeout_seconds: 600
  retry: 1
  dart_defines: "APP_ENV=local_home"

precondition: "flutter_test_env,ack_path_ready"
assertions: "exit_code==0,ack_flow_passed"
ui_ux_checks: "ack_status_transition_correct"

steps:
  - "准备测试环境并校验前置条件：flutter_test_env,ack_path_ready"
  - "执行 Flutter 测试：flutter test test/integration/message_ack_flow_integration_test.dart"
  - "校验断言与 UI/UX 关键点：exit_code==0,ack_flow_passed | ack_status_transition_correct"
